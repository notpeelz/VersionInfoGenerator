namespace VersionInfoGenerator
{
    // This class only exists so that the VersionInfoGenerator project isn't
    // completely empty; without any .cs files, the MSBuild SDK doesn't
    // generate assembly attributes.
    internal static class VersionInfoStub { }
}
